# Huly Self-Hosted Environment Variables Template
# Copy this file to configure your Coolify deployment
# 
# IMPORTANT: Never commit actual secrets to Git!
# Use this as reference for setting up Coolify environment variables

# =============================================================================
# 🚨 CRITICAL SECRETS (Set these in Coolify, NEVER commit to Git!)
# =============================================================================

# Database password - Generate with: openssl rand -hex 32
CR_USER_PASSWORD=your_secure_database_password_here

# Message queue password - Generate with: openssl rand -hex 32  
REDPANDA_ADMIN_PWD=your_secure_redpanda_password_here

# Application secret - Generate with: openssl rand -hex 32
SECRET=your_secure_32_character_secret_here

# Constructed database URL (auto-built, don't modify)
CR_DB_URL=postgres://huly_prod:your_secure_database_password_here@cockroach:26257/defaultdb

# =============================================================================
# 🌐 DOMAIN CONFIGURATION
# =============================================================================

# Your domain or IP address (without http/https)
# Examples: 
#   - huly.yourdomain.com (when domain is ready)
#   - 123.456.789.123 (for initial IP-based access)
#   - 123-456-789-123.nip.io (for temporary domain)
HOST_ADDRESS=your-vps-ip-or-domain

# SSL configuration (set to 's' for HTTPS, leave empty for HTTP)
# Will be 's' when domain + SSL are configured
SECURE=

# =============================================================================
# 📱 APPLICATION SETTINGS
# =============================================================================

HULY_VERSION=s0.7.204
TITLE=Huly Self-Hosted
DEFAULT_LANGUAGE=en
LAST_NAME_FIRST=true

# Database configuration
CR_DATABASE=defaultdb
CR_USERNAME=huly_prod

# Message queue configuration  
REDPANDA_ADMIN_USER=admin

# =============================================================================
# 📧 EMAIL CONFIGURATION (Optional - Choose ONE method)
# =============================================================================

# Method 1: SMTP (Gmail, Outlook, etc.)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your_app_specific_password
# MAIL_FROM=noreply@yourdomain.com
# MAIL_URL=http://mail:8097

# Method 2: Amazon SES
# SES_ACCESS_KEY=your_ses_access_key
# SES_SECRET_KEY=your_ses_secret_key
# SES_REGION=us-east-1
# MAIL_FROM=noreply@yourdomain.com
# MAIL_URL=http://mail:8097

# =============================================================================
# 🔔 PUSH NOTIFICATIONS (Optional)
# =============================================================================

# Generate with: npx web-push generate-vapid-keys
# PUSH_PUBLIC_KEY=your_vapid_public_key
# PUSH_PRIVATE_KEY=your_vapid_private_key
# WEB_PUSH_URL=http://notification:3335

# =============================================================================
# 🔐 OAUTH AUTHENTICATION (Optional)
# =============================================================================

# GitHub OAuth
# GITHUB_CLIENT_ID=your_github_client_id  
# GITHUB_CLIENT_SECRET=your_github_client_secret

# OpenID Connect (Auth0, Okta, etc.)
# OPENID_CLIENT_ID=your_oidc_client_id
# OPENID_CLIENT_SECRET=your_oidc_client_secret
# OPENID_ISSUER=https://your-oidc-provider.com

# =============================================================================
# 🤖 AI INTEGRATION (Optional)
# =============================================================================

# OpenAI Configuration
# OPENAI_API_KEY=your_openai_api_key
# OPENAI_BASE_URL=https://api.openai.com/v1
# AI_URL=http://aibot:4010
# AI_BOT_URL=http://aibot:4010
# AI_BOT_PASSWORD=your_secure_bot_password

# =============================================================================
# 📹 VIDEO CALLS (Optional)
# =============================================================================

# LiveKit Configuration for video/audio calls
# LIVEKIT_HOST=wss://your-livekit-server.livekit.cloud
# LIVEKIT_API_KEY=your_livekit_api_key
# LIVEKIT_API_SECRET=your_livekit_api_secret
# LOVE_ENDPOINT=http://love:8096

# =============================================================================
# 🔒 SECURITY SETTINGS (Optional)
# =============================================================================

# Disable public signups (invite-only mode)
# DISABLE_SIGNUP=true

# =============================================================================
# ⚙️ COOLIFY DEPLOYMENT STEPS
# =============================================================================

# 1. Create new resource in Coolify from your GitHub repository
# 2. Set "front" as main service on port 8080
# 3. Copy the CRITICAL SECRETS section to Coolify environment variables
# 4. Configure HOST_ADDRESS with your VPS IP initially
# 5. Add optional services as needed
# 6. Deploy and verify all services start correctly
# 7. Create your first admin account at http://your-vps-ip
# 8. When domain is ready:
#    - Update HOST_ADDRESS to your domain
#    - Set SECURE=s
#    - Enable SSL in Coolify
#    - Redeploy

# =============================================================================
# 🚨 SECURITY REMINDERS
# =============================================================================

# ❌ NEVER commit files containing real passwords or secrets to Git
# ✅ Always use Coolify environment variables for sensitive data
# ✅ Generate strong, unique passwords for each service
# ✅ Regularly update HULY_VERSION for security patches
# ✅ Use HTTPS only in production (after domain setup)
# ✅ Consider enabling DISABLE_SIGNUP for better security
# ✅ Set up regular backups of persistent data